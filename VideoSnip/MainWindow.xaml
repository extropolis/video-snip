<Window x:Class="VideoSnip.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:VideoSnip.Models"
        Title="Video Snip"
        Icon="icon.ico"
        SizeToContent="WidthAndHeight"
        MinWidth="550"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanMinimize"
        Topmost="True"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <!-- Button Style -->
        <Style x:Key="ToolbarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#AAAAAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem Style for dropdown items -->
        <Style x:Key="DarkComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#007ACC"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#007ACC"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="ToolbarComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource DarkComboBoxItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="3">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0"/>
                                                <Path Grid.Column="1"
                                                      Data="M 0 0 L 4 4 L 8 0 Z"
                                                      Fill="#333333"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <TextBlock x:Name="ContentSite"
                                       IsHitTestVisible="False"
                                       Text="{Binding SelectedItem.Name, RelativeSource={RelativeSource TemplatedParent}}"
                                       Foreground="{TemplateBinding Foreground}"
                                       Margin="8,6,24,6"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder"
                                        Background="#FFFFFF"
                                        BorderBrush="#CCCCCC"
                                        BorderThickness="1"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Stop Button Style (Red) -->
        <Style x:Key="StopButton" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="Foreground" Value="#D32F2F"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFEBEE"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="12,8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="1"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="1"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="1"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="1"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Full Screen Button (default/first option) -->
        <Button x:Name="BtnFullScreen"
                Grid.Column="0"
                Style="{StaticResource ToolbarButton}"
                VerticalAlignment="Center"
                Click="BtnFullScreen_Click"
                ToolTip="Record entire screen">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="&#xE740;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                <TextBlock Text="Full Screen" VerticalAlignment="Center"/>
            </StackPanel>
        </Button>

        <!-- Separator -->
        <Rectangle Grid.Column="1" Fill="#CCCCCC" Width="1" Margin="4,6" VerticalAlignment="Stretch"/>

        <!-- Window Button -->
        <Button x:Name="BtnNewWindow"
                Grid.Column="2"
                Style="{StaticResource ToolbarButton}"
                VerticalAlignment="Center"
                Click="BtnNewWindow_Click"
                ToolTip="Click on window to record">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="&#xE8A7;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                <TextBlock Text="Window" VerticalAlignment="Center"/>
            </StackPanel>
        </Button>

        <!-- Separator -->
        <Rectangle Grid.Column="3" Fill="#CCCCCC" Width="1" Margin="4,6" VerticalAlignment="Stretch"/>

        <!-- Region Button (adjacent to aspect ratio) -->
        <Button x:Name="BtnNewRegion"
                Grid.Column="4"
                Style="{StaticResource ToolbarButton}"
                VerticalAlignment="Center"
                Click="BtnNewRegion_Click"
                ToolTip="Select region to record (drag)">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="&#xE7C4;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                <TextBlock Text="Region" VerticalAlignment="Center"/>
            </StackPanel>
        </Button>

        <!-- Aspect Ratio Dropdown (right next to Region) -->
        <ComboBox x:Name="AspectRatioCombo"
                  Grid.Column="5"
                  Style="{StaticResource ToolbarComboBox}"
                  Width="130"
                  Height="28"
                  VerticalAlignment="Center"
                  VerticalContentAlignment="Center"
                  ToolTip="Aspect ratio preset (applies to Region mode)">
        </ComboBox>

        <!-- Separator -->
        <Rectangle Grid.Column="6" Fill="#CCCCCC" Width="1" Margin="4,6" VerticalAlignment="Stretch"/>

        <!-- Pause Button -->
        <Button x:Name="BtnPause"
                Grid.Column="7"
                Style="{StaticResource ToolbarButton}"
                VerticalAlignment="Center"
                Click="BtnPause_Click"
                IsEnabled="False"
                ToolTip="Pause/Resume recording (Ctrl+Shift+P)">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock x:Name="PauseIcon" Text="&#xE769;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                <TextBlock x:Name="PauseText" Text="Pause" VerticalAlignment="Center"/>
            </StackPanel>
        </Button>

        <!-- Restart Button -->
        <Button x:Name="BtnRestart"
                Grid.Column="8"
                Style="{StaticResource ToolbarButton}"
                VerticalAlignment="Center"
                Click="BtnRestart_Click"
                IsEnabled="False"
                ToolTip="Restart recording (discard current and start fresh)">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                <TextBlock Text="Restart" VerticalAlignment="Center"/>
            </StackPanel>
        </Button>

        <!-- Stop Button -->
        <Button x:Name="BtnStop"
                Grid.Column="9"
                Style="{StaticResource StopButton}"
                VerticalAlignment="Center"
                Click="BtnStop_Click"
                IsEnabled="False"
                ToolTip="Stop recording (Ctrl+Shift+S)">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="&#xE71A;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                <TextBlock Text="Stop" VerticalAlignment="Center"/>
            </StackPanel>
        </Button>

        <!-- Duration Display -->
        <TextBlock x:Name="DurationText"
                   Grid.Column="10"
                   Text=""
                   Foreground="#D32F2F"
                   FontFamily="Consolas"
                   FontSize="14"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"/>

        <!-- Recording Indicator -->
        <Ellipse x:Name="RecordingIndicator"
                 Grid.Column="11"
                 Width="10" Height="10"
                 Fill="#FF4444"
                 Visibility="Collapsed"
                 Margin="0,0,8,0"/>

        <!-- Separator -->
        <Rectangle Grid.Column="12" Fill="#CCCCCC" Width="1" Margin="4,6" VerticalAlignment="Stretch"/>

        <!-- Exit Button -->
        <Button x:Name="BtnExit"
                Grid.Column="13"
                Style="{StaticResource ToolbarButton}"
                VerticalAlignment="Center"
                Click="BtnExit_Click"
                ToolTip="Exit application">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="&#xE7E8;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                <TextBlock Text="Exit" VerticalAlignment="Center"/>
            </StackPanel>
        </Button>

    </Grid>
</Window>
